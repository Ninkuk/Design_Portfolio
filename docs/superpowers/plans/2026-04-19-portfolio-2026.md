# Portfolio 2026 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rebuild the 2020 graphic design portfolio as a 2026 editorial-style static site on GitHub Pages, with 2 case studies (Drive Insight, Blue Voice), an ASU Hub gallery, and a filterable archive that preserves every 2020 piece.

**Architecture:** Static HTML + CSS + vanilla JS. No build step, no framework. CSS split into `tokens → base → components → responsive` (cascade order). Archive is data-driven via `data/work.json`. Shared `lightbox.js` serves both `/work` and `/asu-hub`.

**Tech Stack:** HTML5, CSS3 custom properties, vanilla JS (ES2020+), Google Fonts (Instrument Serif + Inter). Hosted on GitHub Pages. Preview via `python3 -m http.server 8000`. Playwright MCP available for automated browser checks on JS behavior.

**Spec reference:** `docs/superpowers/specs/2026-04-19-portfolio-2026-design.md`

**TDD adaptation note:** Static site has no unit test framework. "Tests" in this plan mean either (a) **browser preview** (load the page at `http://localhost:8000`, verify visually) or (b) **Playwright MCP check** (for JS interactions like filter chips and lightbox). Both are treated as the test-passing step.

**JS authoring note:** All DOM construction in this plan uses `document.createElement` + `textContent` + `setAttribute` rather than `innerHTML`. The archive data is owner-authored and not untrusted, but avoiding innerHTML keeps the code safe by default and sidesteps repository security hooks.

**Media policy (from the user):** Media and asset files under `images/` and `animations/` MAY be renamed (e.g., to match kebab-case IDs in `data/work.json`). They MUST NOT be deleted under any circumstance. Renames should be done with `git mv` so history is preserved. This applies throughout Tasks 9, 16, 17, and any future execution steps touching assets.

---

## Phase 1 — Foundations

### Task 1: CSS token system + fonts

**Files:**
- Create: `css/tokens.css`
- Create: `_preview/tokens-check.html` (throwaway visual verification page; deleted at end of task)

- [ ] **Step 1: Write `css/tokens.css`**

```css
/* Editorial palette — cream + near-black + modernized teal */
:root {
  /* Colors */
  --bg: #F4F1EA;
  --text: #1A1A1A;
  --text-muted: #5A5A5A;
  --rule: #E6E2D8;
  --accent: #00796B;
  --accent-hover: #005b50;

  /* Typography */
  --font-display: "Instrument Serif", Georgia, "Times New Roman", serif;
  --font-body: "Inter", -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;

  /* Type scale (modular, 1.25 ratio) */
  --fs-xs: 0.75rem;
  --fs-sm: 0.875rem;
  --fs-base: 1rem;
  --fs-lg: 1.25rem;
  --fs-xl: 1.75rem;
  --fs-2xl: 2.5rem;
  --fs-3xl: 3.75rem;
  --fs-4xl: 5rem;

  /* Spacing scale (8px base) */
  --sp-1: 0.5rem;
  --sp-2: 1rem;
  --sp-3: 1.5rem;
  --sp-4: 2rem;
  --sp-5: 3rem;
  --sp-6: 4rem;
  --sp-7: 6rem;
  --sp-8: 8rem;

  /* Layout */
  --content-max: 1200px;
  --gutter: 2rem;
  --prose-max: 65ch;
}
```

- [ ] **Step 2: Write `_preview/tokens-check.html` for visual verification**

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>Tokens Check</title>
  <link href="https://fonts.googleapis.com/css2?family=Instrument+Serif:ital@0;1&family=Inter:wght@400;500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="../css/tokens.css">
  <style>
    body { background: var(--bg); color: var(--text); font-family: var(--font-body); padding: 2rem; }
    .swatch { display:inline-block; width:80px; height:80px; margin-right:12px; vertical-align:top; }
    .label { font-size: var(--fs-xs); text-transform: uppercase; letter-spacing: 2px; color: var(--text-muted); }
    h1 { font-family: var(--font-display); font-size: var(--fs-3xl); letter-spacing: -0.02em; margin: 1rem 0; }
  </style>
</head>
<body>
  <h1>Tokens Check</h1>
  <p style="font-family: var(--font-display); font-size: var(--fs-2xl);">Instrument Serif at 2.5rem.</p>
  <p>Inter at 1rem body — <a style="color: var(--accent);">link color</a>.</p>
  <div>
    <div class="swatch" style="background: var(--bg); border:1px solid var(--rule);"></div>
    <div class="swatch" style="background: var(--text);"></div>
    <div class="swatch" style="background: var(--text-muted);"></div>
    <div class="swatch" style="background: var(--rule);"></div>
    <div class="swatch" style="background: var(--accent);"></div>
    <div class="swatch" style="background: var(--accent-hover);"></div>
  </div>
</body>
</html>
```

- [ ] **Step 3: Preview and verify**

Start server: `python3 -m http.server 8000` from repo root. Open `http://localhost:8000/_preview/tokens-check.html`. Verify: cream background, near-black text, Instrument Serif renders, Inter renders, accent swatch is teal (#00796B).

- [ ] **Step 4: Delete the preview file**

```bash
rm _preview/tokens-check.html
rmdir _preview
```

- [ ] **Step 5: Commit**

```bash
git add css/tokens.css
git commit -m "feat(css): add editorial design token system"
```

---

### Task 2: Base CSS — reset, typography, layout primitives

**Files:**
- Create: `css/base.css`

- [ ] **Step 1: Write `css/base.css`**

```css
/* Minimal reset */
*, *::before, *::after { box-sizing: border-box; }
html, body, h1, h2, h3, h4, p, ul, ol, figure { margin: 0; padding: 0; }
ul, ol { list-style: none; }
img, video { display: block; max-width: 100%; height: auto; }
a { color: inherit; text-decoration: none; }
button { font: inherit; background: none; border: none; cursor: pointer; color: inherit; }

/* Base typography */
html { font-size: 16px; -webkit-font-smoothing: antialiased; -moz-osx-font-smoothing: grayscale; }
body {
  background: var(--bg);
  color: var(--text);
  font-family: var(--font-body);
  font-size: var(--fs-base);
  line-height: 1.55;
}

/* Display headings */
h1, h2, h3, .display {
  font-family: var(--font-display);
  font-weight: 400;
  line-height: 1.05;
  letter-spacing: -0.015em;
}
h1 { font-size: var(--fs-3xl); }
h2 { font-size: var(--fs-2xl); }
h3 { font-size: var(--fs-xl); }

/* Prose */
p { max-width: var(--prose-max); margin-bottom: var(--sp-2); }
p.lead { font-size: var(--fs-lg); color: var(--text); }
a:hover, a:focus { color: var(--accent-hover); }

/* Small UI text */
.label {
  font-size: var(--fs-xs);
  text-transform: uppercase;
  letter-spacing: 0.15em;
  font-weight: 500;
  color: var(--text-muted);
}
.muted { color: var(--text-muted); }

/* Layout primitives */
.container { max-width: var(--content-max); margin: 0 auto; padding: 0 var(--gutter); }
.container-narrow { max-width: 720px; margin: 0 auto; padding: 0 var(--gutter); }

.rule { height: 1px; background: var(--rule); border: none; margin: var(--sp-5) 0; }
.accent-rule { height: 2px; width: 40px; background: var(--accent); border: none; margin: var(--sp-2) 0; }

section { padding: var(--sp-6) 0; }
section + section { padding-top: 0; }
```

- [ ] **Step 2: Verification deferred to Task 3**

No standalone verification. Task 3 validates tokens + base together on `about.html`.

- [ ] **Step 3: Commit**

```bash
git add css/base.css
git commit -m "feat(css): add base typography and layout primitives"
```

---

### Task 3: Rebuild `about.html` on new visual system (validation page)

**Files:**
- Modify: `about.html` (full rewrite)

- [ ] **Step 1: Replace `about.html` contents**

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>About — Ninad Kulkarni</title>
  <meta name="description" content="Designer and software engineer. Brand systems, print, and product UI.">
  <link rel="icon" type="image/jpeg" href="./favicon.jpg">
  <link href="https://fonts.googleapis.com/css2?family=Instrument+Serif:ital@0;1&family=Inter:wght@400;500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="./css/tokens.css">
  <link rel="stylesheet" href="./css/base.css">
</head>
<body>
  <header class="container" style="padding-top: var(--sp-4); padding-bottom: var(--sp-5);">
    <a href="./index.html" class="label" style="color: var(--accent);">&larr; Ninad Kulkarni</a>
  </header>

  <main class="container-narrow">
    <div class="label">About</div>
    <h1 style="font-size: var(--fs-3xl); margin-top: var(--sp-2);">Designer. Sometimes engineer.</h1>
    <hr class="accent-rule">

    <p class="lead" style="margin-top: var(--sp-4);">
      <!-- TODO during execution: replace with final bio. -->
      I'm a full-stack software engineer at Blue Voice and a designer working across brand, print,
      and product. I spent a season designing for ASU's Enterprise Marketing Hub (Alumni and Athletics,
      print and digital), and founded Drive Insight, where I owned everything from logo to product UI.
    </p>

    <p>
      Before all that: I studied Computer Science at ASU, spent years nurturing graphic, web, and
      app design skills, and have worked with most of the Adobe Creative Cloud suite.
    </p>

    <p class="muted" style="font-size: var(--fs-sm);">Fun fact: I can solve a Rubik's Cube in under 50 seconds.</p>

    <hr class="rule">

    <div class="label">Selected Achievements</div>
    <ul style="margin-top: var(--sp-2); line-height: 1.9;">
      <!-- TODO: migrate real entries from old achievements.html before deleting it in Task 16. -->
      <li>National Social Media Ambassador, FBLA</li>
      <li>[additional entries]</li>
    </ul>

    <hr class="rule">

    <div class="label">Skills</div>
    <p style="margin-top: var(--sp-2);">
      <!-- TODO: trim aggressively — this is 2026, not "knows Photoshop" territory. -->
      Brand systems &middot; Print design &middot; Product UI/UX &middot; Typography &middot; Photography &middot;
      Figma &middot; Adobe Creative Suite &middot; HTML/CSS/JS &middot; React &middot; TypeScript
    </p>

    <hr class="rule">

    <div class="label">Contact</div>
    <ul style="margin-top: var(--sp-2); line-height: 1.9;">
      <li><a href="mailto:ninadk.dev@gmail.com" style="color: var(--accent);">ninadk.dev@gmail.com</a></li>
      <li><a href="https://www.linkedin.com/in/ninad-kulkarni-2001/" style="color: var(--accent);">LinkedIn</a></li>
      <li><a href="https://www.behance.net/ninadk038247" style="color: var(--accent);">Behance</a></li>
      <li><a href="https://github.com/Ninkuk" style="color: var(--accent);">GitHub</a></li>
    </ul>
  </main>

  <footer class="container" style="padding: var(--sp-6) var(--gutter); color: var(--text-muted); font-size: var(--fs-sm);">
    <hr class="rule" style="margin-bottom: var(--sp-3);">
    Ninad Kulkarni &middot; 2026
  </footer>
</body>
</html>
```

- [ ] **Step 2: Preview**

Open `http://localhost:8000/about.html`. Verify cream background, serif heading "Designer. Sometimes engineer.", accent rule under heading, teal links, editorial feel.

- [ ] **Step 3: Commit**

```bash
git add about.html
git commit -m "feat(about): rebuild about page on new editorial system"
```

---

### Task 4: Build `index.html` homepage in new system (skeleton)

**Files:**
- Modify: `index.html` (full rewrite)
- Create: `css/components.css`

- [ ] **Step 1: Create `css/components.css` with case study tile styles**

```css
/* Case study tile (homepage) */
.case-study-tile { display: block; }
.case-study-tile a { display: block; color: inherit; }
.case-study-tile__image {
  width: 100%;
  aspect-ratio: 16 / 9;
  background: var(--rule);
  border-radius: 4px;
  overflow: hidden;
  transition: transform 0.2s ease;
}
.case-study-tile__image[data-placeholder] {
  background: repeating-linear-gradient(45deg, var(--rule), var(--rule) 12px, #d8d4c8 12px, #d8d4c8 24px);
}
.case-study-tile__image img { width: 100%; height: 100%; object-fit: cover; display: block; }
.case-study-tile a:hover .case-study-tile__image { transform: translateY(-2px); }
.case-study-tile__meta { padding-top: var(--sp-2); }
.case-study-tile__meta h2 { margin-top: var(--sp-1); }
```

- [ ] **Step 2: Replace `index.html` contents**

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Ninad Kulkarni — Designer</title>
  <meta name="description" content="Designer and software engineer. Brand systems, print, and product UI.">
  <link rel="icon" type="image/jpeg" href="./favicon.jpg">
  <link href="https://fonts.googleapis.com/css2?family=Instrument+Serif:ital@0;1&family=Inter:wght@400;500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="./css/tokens.css">
  <link rel="stylesheet" href="./css/base.css">
  <link rel="stylesheet" href="./css/components.css">
</head>
<body>

  <section class="container" style="padding-top: var(--sp-6); padding-bottom: var(--sp-7);">
    <div class="label">Ninad Kulkarni</div>
    <h1 style="font-size: clamp(2.5rem, 7vw, 5rem); margin-top: var(--sp-3); max-width: 900px;">
      Designer<br>working across brand, print, and product.
    </h1>
    <p class="lead muted" style="margin-top: var(--sp-3); max-width: 580px;">
      Selected work from a founding-designer stint at Drive Insight, ASU Enterprise Marketing,
      and the Blue Voice app I'm redesigning now.
    </p>
  </section>

  <hr class="rule container" style="max-width: var(--content-max);">

  <section class="container">
    <div class="label">Selected Work</div>

    <div class="case-study-tile" style="margin-top: var(--sp-4);">
      <a href="./case-study/drive-insight.html">
        <div class="case-study-tile__image" data-placeholder></div>
        <div class="case-study-tile__meta">
          <div class="label">Case Study &middot; Brand + Product</div>
          <h2>Drive Insight</h2>
          <p class="muted">End-to-end design for a founding-stage startup — brand, product UI, marketing.</p>
        </div>
      </a>
    </div>

    <div class="case-study-tile" style="margin-top: var(--sp-5);">
      <a href="./case-study/app-redesign.html">
        <div class="case-study-tile__image" data-placeholder></div>
        <div class="case-study-tile__meta">
          <div class="label">Case Study &middot; Product UX</div>
          <h2>Blue Voice app redesign</h2>
          <p class="muted">UX-led redesign of the entire Blue Voice app — before, after, and the decisions between.</p>
        </div>
      </a>
    </div>
  </section>

  <hr class="rule container" style="max-width: var(--content-max); margin-top: var(--sp-6);">

  <section class="container">
    <div class="label">Gallery</div>
    <a href="./asu-hub.html" style="display: block; margin-top: var(--sp-3);">
      <h2>ASU Alumni + Athletics &rarr;</h2>
      <p class="muted" style="margin-top: var(--sp-2);">Print and digital campaign work for ASU Enterprise Marketing.</p>
    </a>
  </section>

  <hr class="rule container" style="max-width: var(--content-max); margin-top: var(--sp-6);">

  <section class="container">
    <a href="./work.html" style="display: block;">
      <div class="label">Archive</div>
      <h2 style="margin-top: var(--sp-2);">Everything else &rarr;</h2>
      <p class="muted" style="margin-top: var(--sp-2);">The full catalog — logos, posters, social, photography, and more.</p>
    </a>
  </section>

  <footer class="container" style="padding: var(--sp-7) var(--gutter) var(--sp-4); color: var(--text-muted); font-size: var(--fs-sm);">
    <hr class="rule" style="margin-bottom: var(--sp-3);">
    <div style="display: flex; justify-content: space-between; flex-wrap: wrap; gap: var(--sp-2);">
      <span>Ninad Kulkarni &middot; 2026</span>
      <span>
        <a href="./about.html" style="color: var(--text-muted);">About</a> &middot;
        <a href="mailto:ninadk.dev@gmail.com" style="color: var(--text-muted);">Email</a> &middot;
        <a href="https://www.linkedin.com/in/ninad-kulkarni-2001/" style="color: var(--text-muted);">LinkedIn</a>
      </span>
    </div>
  </footer>
</body>
</html>
```

- [ ] **Step 3: Preview**

Open `http://localhost:8000/`. Expect hero with serif heading, two case study tiles with placeholder stripe images, hover lifts tiles, ASU + Archive links, footer.

- [ ] **Step 4: Commit**

```bash
git add index.html css/components.css
git commit -m "feat(home): rebuild homepage skeleton on new editorial system"
```

---

## Phase 2 — Case Studies

### Task 5: Case study layout components

**Files:**
- Modify: `css/components.css` (append)

- [ ] **Step 1: Append to `css/components.css`**

```css
/* Case study layout */
.cs-header { padding: var(--sp-6) 0 var(--sp-5); }
.cs-header .label { color: var(--accent); }
.cs-header h1 { font-size: clamp(2.5rem, 6vw, 4rem); margin-top: var(--sp-2); max-width: 900px; }
.cs-header .summary { font-size: var(--fs-lg); color: var(--text-muted); margin-top: var(--sp-3); max-width: 640px; }

.cs-meta {
  display: flex;
  gap: var(--sp-5);
  margin-top: var(--sp-4);
  flex-wrap: wrap;
  padding-top: var(--sp-3);
  border-top: 1px solid var(--rule);
  font-size: var(--fs-sm);
}
.cs-meta dt { color: var(--text-muted); text-transform: uppercase; letter-spacing: 0.12em; font-size: var(--fs-xs); font-weight: 500; }
.cs-meta dd { margin-top: 4px; }

.cs-section { max-width: 900px; margin: 0 auto; padding: var(--sp-5) var(--gutter); }
.cs-section .section-label {
  font-size: var(--fs-xs);
  text-transform: uppercase;
  letter-spacing: 0.15em;
  color: var(--accent);
  font-weight: 500;
  margin-bottom: var(--sp-3);
}

.cs-image { margin: var(--sp-4) auto; max-width: 100%; }
.cs-image img { width: 100%; border-radius: 3px; }
.cs-image figcaption { color: var(--text-muted); font-size: var(--fs-sm); margin-top: var(--sp-1); }

.cs-image--full { max-width: none; margin-left: calc(-1 * var(--gutter)); margin-right: calc(-1 * var(--gutter)); }

.cs-grid-2 { display: grid; grid-template-columns: 1fr 1fr; gap: var(--sp-2); margin: var(--sp-4) 0; }

.cs-nav {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: var(--sp-4) 0;
  border-top: 1px solid var(--rule);
  margin-top: var(--sp-6);
}
.cs-nav a { color: var(--accent); }
```

- [ ] **Step 2: Commit**

```bash
git add css/components.css
git commit -m "feat(css): add case study layout components"
```

---

### Task 6: Drive Insight case study page

**Files:**
- Create: `case-study/drive-insight.html`

- [ ] **Step 1: Create `case-study/drive-insight.html`**

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Drive Insight — Case Study &middot; Ninad Kulkarni</title>
  <meta name="description" content="Founding-designer case study: brand, product UI, and marketing for Drive Insight.">
  <link rel="icon" type="image/jpeg" href="../favicon.jpg">
  <link href="https://fonts.googleapis.com/css2?family=Instrument+Serif:ital@0;1&family=Inter:wght@400;500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="../css/tokens.css">
  <link rel="stylesheet" href="../css/base.css">
  <link rel="stylesheet" href="../css/components.css">
</head>
<body>
  <header class="container" style="padding-top: var(--sp-4);">
    <a href="../index.html" class="label" style="color: var(--accent);">&larr; Ninad Kulkarni</a>
  </header>

  <div class="container cs-header">
    <div class="label" style="color: var(--accent);">Case Study &middot; Brand + Product</div>
    <h1>Drive Insight</h1>
    <p class="summary">
      <!-- TODO: ~60-word hook. -->
      End-to-end design for a founding-stage startup — brand system, product UI, marketing surfaces.
      I owned every pixel from logo to landing page to shipping screens.
    </p>

    <dl class="cs-meta">
      <div><dt>Role</dt><dd>Founding Designer</dd></div>
      <div><dt>Scope</dt><dd>Brand &middot; Product UI &middot; Marketing</dd></div>
      <div><dt>Year</dt><dd>[year range]</dd></div>
      <div><dt>Status</dt><dd>On ice</dd></div>
    </dl>
  </div>

  <figure class="cs-section cs-image cs-image--full">
    <!-- TODO: hero image. -->
    <div class="case-study-tile__image" data-placeholder style="aspect-ratio: 16/9;"></div>
  </figure>

  <section class="cs-section">
    <div class="section-label">Context</div>
    <p><!-- TODO: ~100 words. --></p>
  </section>

  <section class="cs-section">
    <div class="section-label">Brand System</div>
    <p><!-- TODO: ~150 words. --></p>
    <div class="cs-grid-2">
      <figure class="cs-image"><div class="case-study-tile__image" data-placeholder></div><figcaption>Logo / mark</figcaption></figure>
      <figure class="cs-image"><div class="case-study-tile__image" data-placeholder></div><figcaption>Type + color</figcaption></figure>
    </div>
  </section>

  <section class="cs-section">
    <div class="section-label">Product UI</div>
    <p><!-- TODO: ~150 words. --></p>
    <figure class="cs-image"><div class="case-study-tile__image" data-placeholder style="aspect-ratio: 3/2;"></div></figure>
    <div class="cs-grid-2">
      <figure class="cs-image"><div class="case-study-tile__image" data-placeholder></div></figure>
      <figure class="cs-image"><div class="case-study-tile__image" data-placeholder></div></figure>
    </div>
  </section>

  <section class="cs-section">
    <div class="section-label">Marketing</div>
    <p><!-- TODO: ~100 words. --></p>
    <figure class="cs-image"><div class="case-study-tile__image" data-placeholder style="aspect-ratio: 3/2;"></div></figure>
  </section>

  <section class="cs-section">
    <div class="section-label">Reflection</div>
    <p><!-- TODO: ~80 words. --></p>
  </section>

  <nav class="container cs-nav">
    <a href="../index.html">&larr; All work</a>
    <a href="./app-redesign.html">Blue Voice app redesign &rarr;</a>
  </nav>

  <footer class="container" style="padding: var(--sp-5) var(--gutter); color: var(--text-muted); font-size: var(--fs-sm);">
    Ninad Kulkarni &middot; 2026
  </footer>
</body>
</html>
```

- [ ] **Step 2: Preview**

Open `http://localhost:8000/case-study/drive-insight.html`. Verify layout, teal section labels, placeholder images, prev/next nav.

- [ ] **Step 3: Commit**

```bash
git add case-study/drive-insight.html
git commit -m "feat(case-study): scaffold Drive Insight case study page"
```

**Note for executor:** Copy and real images are TODOs. Complete them by asking the user — do not invent details about Drive Insight.

---

### Task 7: Blue Voice app redesign case study page

**Files:**
- Create: `case-study/app-redesign.html`

- [ ] **Step 1: Create `case-study/app-redesign.html`**

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Blue Voice App Redesign — Case Study &middot; Ninad Kulkarni</title>
  <meta name="description" content="UX-led redesign of the Blue Voice app. Before, after, and the decisions between.">
  <link rel="icon" type="image/jpeg" href="../favicon.jpg">
  <link href="https://fonts.googleapis.com/css2?family=Instrument+Serif:ital@0;1&family=Inter:wght@400;500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="../css/tokens.css">
  <link rel="stylesheet" href="../css/base.css">
  <link rel="stylesheet" href="../css/components.css">
</head>
<body>
  <header class="container" style="padding-top: var(--sp-4);">
    <a href="../index.html" class="label" style="color: var(--accent);">&larr; Ninad Kulkarni</a>
  </header>

  <div class="container cs-header">
    <div class="label" style="color: var(--accent);">Case Study &middot; Product UX</div>
    <h1>Blue Voice app redesign</h1>
    <p class="summary">
      <!-- TODO: ~60-word hook. -->
      A UX-led revamp of the entire Blue Voice mobile app — reframed the information architecture,
      rebuilt the core flows, and shipped it alongside my day-to-day engineering work.
    </p>

    <dl class="cs-meta">
      <div><dt>Role</dt><dd>Designer (alongside SWE)</dd></div>
      <div><dt>Scope</dt><dd>UX &middot; Product UI</dd></div>
      <div><dt>Year</dt><dd>2025</dd></div>
      <div><dt>Status</dt><dd>Shipped</dd></div>
    </dl>
  </div>

  <figure class="cs-section cs-image cs-image--full">
    <!-- TODO: before/after hero. -->
    <div class="case-study-tile__image" data-placeholder style="aspect-ratio: 16/9;"></div>
  </figure>

  <section class="cs-section">
    <div class="section-label">Problem</div>
    <p><!-- TODO: ~120 words. --></p>
  </section>

  <section class="cs-section">
    <div class="section-label">Key Decisions</div>
    <p><!-- TODO: ~60 word intro. --></p>
    <ol style="list-style: decimal; padding-left: 1.5rem; margin-top: var(--sp-2);">
      <li style="margin-bottom: var(--sp-3);"><strong>Decision 1</strong><p><!-- TODO: ~80 words --></p></li>
      <li style="margin-bottom: var(--sp-3);"><strong>Decision 2</strong><p><!-- TODO: ~80 words --></p></li>
      <li><strong>Decision 3</strong><p><!-- TODO: ~80 words --></p></li>
    </ol>
  </section>

  <section class="cs-section">
    <div class="section-label">Final Screens</div>
    <div class="cs-grid-2">
      <figure class="cs-image"><div class="case-study-tile__image" data-placeholder></div></figure>
      <figure class="cs-image"><div class="case-study-tile__image" data-placeholder></div></figure>
      <figure class="cs-image"><div class="case-study-tile__image" data-placeholder></div></figure>
      <figure class="cs-image"><div class="case-study-tile__image" data-placeholder></div></figure>
    </div>
  </section>

  <section class="cs-section">
    <div class="section-label">Outcome</div>
    <p><!-- TODO: ~80 words — measurable if available, qualitative otherwise. --></p>
  </section>

  <section class="cs-section">
    <div class="section-label">Reflection</div>
    <p><!-- TODO: ~80 words. --></p>
  </section>

  <nav class="container cs-nav">
    <a href="./drive-insight.html">&larr; Drive Insight</a>
    <a href="../index.html">All work &rarr;</a>
  </nav>

  <footer class="container" style="padding: var(--sp-5) var(--gutter); color: var(--text-muted); font-size: var(--fs-sm);">
    Ninad Kulkarni &middot; 2026
  </footer>
</body>
</html>
```

- [ ] **Step 2: Preview + verify**

Same check as Task 6 at the Blue Voice URL.

- [ ] **Step 3: Commit**

```bash
git add case-study/app-redesign.html
git commit -m "feat(case-study): scaffold Blue Voice app redesign case study page"
```

---

## Phase 3 — Galleries & Archive

### Task 8: ASU Hub gallery page

**Files:**
- Create: `asu-hub.html`
- Modify: `css/components.css` (append)

- [ ] **Step 1: Append gallery grid styles to `css/components.css`**

```css
/* Gallery grid — used by ASU Hub and /work */
.gallery-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
  gap: var(--sp-2);
  margin-top: var(--sp-4);
}
.gallery-tile {
  background: var(--rule);
  aspect-ratio: 4 / 3;
  cursor: pointer;
  overflow: hidden;
  border-radius: 2px;
  transition: transform 0.15s ease, box-shadow 0.15s ease;
}
.gallery-tile:hover {
  transform: translateY(-2px);
  box-shadow: 0 4px 20px rgba(0,0,0,0.08);
}
.gallery-tile img { width: 100%; height: 100%; object-fit: cover; display: block; }
```

- [ ] **Step 2: Create `asu-hub.html`**

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>ASU Alumni + Athletics — Ninad Kulkarni</title>
  <meta name="description" content="Print and digital design work for ASU Enterprise Marketing — Alumni and Athletics campaigns.">
  <link rel="icon" type="image/jpeg" href="./favicon.jpg">
  <link href="https://fonts.googleapis.com/css2?family=Instrument+Serif:ital@0;1&family=Inter:wght@400;500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="./css/tokens.css">
  <link rel="stylesheet" href="./css/base.css">
  <link rel="stylesheet" href="./css/components.css">
</head>
<body>
  <header class="container" style="padding-top: var(--sp-4);">
    <a href="./index.html" class="label" style="color: var(--accent);">&larr; Ninad Kulkarni</a>
  </header>

  <div class="container cs-header">
    <div class="label" style="color: var(--accent);">Gallery</div>
    <h1>ASU Alumni + Athletics</h1>
    <p class="summary">
      <!-- TODO: ~80-word intro. -->
      Selected design work from my time at ASU's Enterprise Marketing Hub — Alumni and Athletics
      campaigns across print and digital.
    </p>

    <dl class="cs-meta">
      <div><dt>Role</dt><dd>Student Designer</dd></div>
      <div><dt>Scope</dt><dd>Print &middot; Digital &middot; Campaign</dd></div>
      <div><dt>Year</dt><dd>[year range]</dd></div>
    </dl>
  </div>

  <main class="container">
    <div class="gallery-grid" id="asu-gallery">
      <!-- TODO: populate with real ASU Hub images. Each tile: -->
      <!-- <div class="gallery-tile" data-full="./images/ASU/piece-01.jpg"><img src="./images/ASU/piece-01.jpg" alt="Piece title" loading="lazy"></div> -->
    </div>
  </main>

  <footer class="container" style="padding: var(--sp-6) var(--gutter) var(--sp-4); color: var(--text-muted); font-size: var(--fs-sm);">
    <hr class="rule" style="margin-bottom: var(--sp-3);">
    <a href="./index.html" style="color: var(--text-muted);">&larr; Back to all work</a>
  </footer>

  <script src="./js/lightbox.js" defer></script>
</body>
</html>
```

- [ ] **Step 3: Preview**

Open `http://localhost:8000/asu-hub.html`. Grid will be empty until ASU images are provided; page itself should render correctly.

- [ ] **Step 4: Commit**

```bash
git add asu-hub.html css/components.css
git commit -m "feat(asu-hub): scaffold ASU Alumni + Athletics gallery page"
```

**Note for executor:** ASU images don't exist yet. Coordinate with user to place curated images under `./images/ASU/`.

---

### Task 9: Create `data/work.json` archive manifest

**Files:**
- Create: `data/work.json`

- [ ] **Step 1: Inventory existing `images/` subfolders**

Run (or Glob):
```bash
ls images/logos/ images/Manip/ images/Photography/ images/Posters/ images/Restorations/ images/Social/ images/T-Shirts/ images/WebApp/
```

- [ ] **Step 2: Populate `data/work.json`**

Schema:
```json
[
  {
    "id": "string-kebab-id",
    "title": "Human-readable title",
    "tags": ["logo", "brand"],
    "year": 2019,
    "thumb": "./images/logos/file.jpg",
    "full": "./images/logos/file.jpg",
    "caption": ""
  }
]
```

**Tag vocabulary (exhaustive):**
`brand · logo · print · editorial · social · photography · motion · product-ui · image-manipulation · tshirt · restoration · web-app`

**Rules:**
- Every entry has at least one tag
- Multi-tag where genuinely fits
- Use `""` for unknown captions; best-guess year if uncertain and flag to user
- Exclude `.psd` files and root-level `Card_*.jpg` (those were 2020 homepage cards)

**Optional: normalize filenames (user-approved).** The user has allowed renaming media files (but never deleting them). If it helps, use `git mv` to rename source files to match their kebab-case `id` — e.g., `images/logos/N_Logo_Gradient_Insta.jpg` → `images/logos/ninad-logo-gradient.jpg`. Keep the original folder structure; only change filenames. If renaming, update the corresponding `thumb`/`full` paths in the JSON to match. Skip normalization for any file where the existing name is already clean.

Starter entries (executor: finish this list by iterating through each `images/<subfolder>/` file):

```json
[
  { "id": "curry-coop-logo", "title": "CurryCoop Logo", "tags": ["logo", "brand"], "year": 2019, "thumb": "./images/logos/currycoop.jpg", "full": "./images/logos/currycoop.jpg", "caption": "" },
  { "id": "ninad-logo-gradient", "title": "Personal Mark — Gradient", "tags": ["logo", "brand"], "year": 2019, "thumb": "./images/logos/N_Logo_Gradient_Insta.jpg", "full": "./images/logos/N_Logo_Gradient_Insta.jpg", "caption": "" },
  { "id": "rubiks-cube-push", "title": "Rubik's Cube Push", "tags": ["image-manipulation"], "year": 2019, "thumb": "./images/Manip/RubiksCubePush.jpg", "full": "./images/Manip/RubiksCubePush.jpg", "caption": "" },
  { "id": "duotone-1", "title": "Duotone Study 1", "tags": ["image-manipulation", "editorial"], "year": 2019, "thumb": "./images/Manip/Duotone_1.jpg", "full": "./images/Manip/Duotone_1.jpg", "caption": "" },
  { "id": "balloon-tiltshift", "title": "Balloon — Tilt Shift", "tags": ["photography"], "year": 2019, "thumb": "./images/Photography/BalloonTiltShift.jpg", "full": "./images/Photography/BalloonTiltShift.jpg", "caption": "" },
  { "id": "apush-movie-poster", "title": "APUSH Movie Poster — Powhatans v. Colonists", "tags": ["print"], "year": 2018, "thumb": "./images/Posters/APUSH_MoviePoster_Powhatans_v_Colonists.jpg", "full": "./images/Posters/APUSH_MoviePoster_Powhatans_v_Colonists.jpg", "caption": "" },
  { "id": "restore-nana-red", "title": "Photo Restoration — Nana", "tags": ["restoration"], "year": 2018, "thumb": "./images/Restorations/NANAredRestored.gif", "full": "./images/Restorations/NANAredRestored.gif", "caption": "" },
  { "id": "fbla-week-start", "title": "FBLA Week — Start", "tags": ["social"], "year": 2018, "thumb": "./images/Social/FBLA_Week_START.jpg", "full": "./images/Social/FBLA_Week_START.jpg", "caption": "" },
  { "id": "dcon-mockup-tshirt", "title": "DCON T-Shirt Mockup", "tags": ["tshirt"], "year": 2018, "thumb": "./images/T-Shirts/DCON_Mockup.jpg", "full": "./images/T-Shirts/DCON_Mockup.jpg", "caption": "" },
  { "id": "pvlsites", "title": "PVL Sites — Web Design", "tags": ["web-app"], "year": 2019, "thumb": "./images/WebApp/pvlsites.jpg", "full": "./images/WebApp/pvlsites.jpg", "caption": "" }
]
```

Executor: complete this list by adding every file from each `images/<subfolder>/`.

- [ ] **Step 3: Validate JSON**

```bash
python3 -c "import json; print(len(json.load(open('data/work.json'))), 'entries')"
```

Expected: exit code 0, prints a count.

- [ ] **Step 4: Commit**

```bash
git add data/work.json
git commit -m "feat(work): inventory 2020 archive into data/work.json"
```

---

### Task 10: `/work` filterable archive page

**Files:**
- Create: `work.html`
- Modify: `css/components.css` (append)

- [ ] **Step 1: Append filter chip styles to `css/components.css`**

```css
/* Filter chips */
.filter-chips { display: flex; gap: var(--sp-1); flex-wrap: wrap; margin: var(--sp-4) 0 var(--sp-3); }
.chip {
  font-size: var(--fs-sm);
  padding: 6px 14px;
  border: 1px solid var(--rule);
  border-radius: 999px;
  background: transparent;
  color: var(--text-muted);
  cursor: pointer;
  transition: background 0.12s, color 0.12s, border-color 0.12s;
}
.chip:hover { border-color: var(--text-muted); color: var(--text); }
.chip[aria-pressed="true"] { background: var(--accent); color: var(--bg); border-color: var(--accent); }
.chip .count { opacity: 0.7; margin-left: 6px; font-feature-settings: "tnum"; }

.archive-summary { color: var(--text-muted); font-size: var(--fs-sm); margin-top: var(--sp-2); }
```

- [ ] **Step 2: Create `work.html`**

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Work — Ninad Kulkarni</title>
  <meta name="description" content="Complete archive of design work — logos, print, social, photography, and more.">
  <link rel="icon" type="image/jpeg" href="./favicon.jpg">
  <link href="https://fonts.googleapis.com/css2?family=Instrument+Serif:ital@0;1&family=Inter:wght@400;500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="./css/tokens.css">
  <link rel="stylesheet" href="./css/base.css">
  <link rel="stylesheet" href="./css/components.css">
</head>
<body>
  <header class="container" style="padding-top: var(--sp-4);">
    <a href="./index.html" class="label" style="color: var(--accent);">&larr; Ninad Kulkarni</a>
  </header>

  <div class="container cs-header">
    <div class="label" style="color: var(--accent);">Archive</div>
    <h1>All Work</h1>
    <p class="archive-summary" id="archive-summary">Loading&hellip;</p>

    <div class="filter-chips" id="filter-chips" role="group" aria-label="Filter work by category">
      <!-- rendered by js/archive.js -->
    </div>
  </div>

  <main class="container">
    <div class="gallery-grid" id="archive-grid"></div>
  </main>

  <footer class="container" style="padding: var(--sp-6) var(--gutter) var(--sp-4); color: var(--text-muted); font-size: var(--fs-sm);">
    <hr class="rule" style="margin-bottom: var(--sp-3);">
    <a href="./index.html" style="color: var(--text-muted);">&larr; Back to all work</a>
  </footer>

  <script src="./js/archive.js" defer></script>
  <script src="./js/lightbox.js" defer></script>
</body>
</html>
```

- [ ] **Step 3: Preview**

Open `http://localhost:8000/work.html`. Expect "Loading…" text and an empty grid — archive.js is next task.

- [ ] **Step 4: Commit**

```bash
git add work.html css/components.css
git commit -m "feat(work): scaffold filterable archive page"
```

---

### Task 11: `js/archive.js` — fetch, render, filter (DOM-based, no innerHTML)

**Files:**
- Create: `js/archive.js`

- [ ] **Step 1: Create `js/archive.js`**

```javascript
(async function () {
  const grid = document.getElementById('archive-grid');
  const chipsContainer = document.getElementById('filter-chips');
  const summary = document.getElementById('archive-summary');

  if (!grid || !chipsContainer || !summary) return;

  const TAG_LABELS = {
    brand: 'Brand',
    logo: 'Logo',
    print: 'Print',
    editorial: 'Editorial',
    social: 'Social',
    photography: 'Photography',
    motion: 'Motion',
    'product-ui': 'Product UI',
    'image-manipulation': 'Image Manipulation',
    tshirt: 'T-Shirts',
    restoration: 'Restorations',
    'web-app': 'Web & App',
  };

  let data = [];
  try {
    const res = await fetch('./data/work.json');
    if (!res.ok) throw new Error('Failed to load work.json: ' + res.status);
    data = await res.json();
  } catch (err) {
    summary.textContent = 'Archive failed to load.';
    console.error(err);
    return;
  }

  data.sort((a, b) => (b.year || 0) - (a.year || 0));

  const tagCounts = { all: data.length };
  for (const item of data) {
    for (const tag of item.tags || []) {
      tagCounts[tag] = (tagCounts[tag] || 0) + 1;
    }
  }

  function buildChip(tag, isPressed) {
    const btn = document.createElement('button');
    btn.className = 'chip';
    btn.dataset.tag = tag;
    btn.setAttribute('aria-pressed', isPressed ? 'true' : 'false');
    const label = tag === 'all' ? 'All' : TAG_LABELS[tag];
    btn.appendChild(document.createTextNode(label));
    const count = document.createElement('span');
    count.className = 'count';
    count.textContent = String(tagCounts[tag]);
    btn.appendChild(count);
    return btn;
  }

  // Render chips: 'all' first, then tags present in data in the TAG_LABELS order
  chipsContainer.replaceChildren();
  chipsContainer.appendChild(buildChip('all', true));
  for (const tag of Object.keys(TAG_LABELS)) {
    if (tagCounts[tag]) chipsContainer.appendChild(buildChip(tag, false));
  }

  function buildTile(item) {
    const tile = document.createElement('div');
    tile.className = 'gallery-tile';
    tile.dataset.full = item.full;
    tile.dataset.id = item.id;
    tile.title = item.title;

    const img = document.createElement('img');
    img.src = item.thumb;
    img.alt = item.title;
    img.loading = 'lazy';
    tile.appendChild(img);

    return tile;
  }

  function renderTiles(filter) {
    const filtered = filter === 'all' ? data : data.filter(item => (item.tags || []).includes(filter));

    grid.replaceChildren();
    for (const item of filtered) {
      grid.appendChild(buildTile(item));
    }

    const years = filtered.map(i => i.year).filter(Boolean);
    let yearText = '';
    if (years.length) {
      const min = Math.min(...years);
      const max = Math.max(...years);
      yearText = min === max ? ' · ' + min : ' · ' + min + '–' + max;
    }
    summary.textContent = filtered.length + ' ' + (filtered.length === 1 ? 'piece' : 'pieces') + yearText;
  }

  chipsContainer.addEventListener('click', (e) => {
    const chip = e.target.closest('.chip');
    if (!chip) return;

    chipsContainer.querySelectorAll('.chip').forEach(c => c.setAttribute('aria-pressed', 'false'));
    chip.setAttribute('aria-pressed', 'true');

    renderTiles(chip.dataset.tag);
    document.dispatchEvent(new CustomEvent('archive:rendered'));
  });

  renderTiles('all');
  document.dispatchEvent(new CustomEvent('archive:rendered'));
})();
```

- [ ] **Step 2: Verify in browser**

Open `http://localhost:8000/work.html`. Expect: filter chips render with counts, summary shows "N pieces · YYYY–YYYY", grid populates, clicking a chip filters the grid and highlights the chip in accent color.

Playwright MCP check:
```
navigate to http://localhost:8000/work.html
snapshot → verify chips and grid present
click the "Logo" chip
snapshot → verify only logo tiles render
```

- [ ] **Step 3: Commit**

```bash
git add js/archive.js
git commit -m "feat(work): add filterable archive with chip vocabulary and counts"
```

---

### Task 12: `js/lightbox.js` — image overlay (DOM-based)

**Files:**
- Create: `js/lightbox.js`
- Modify: `css/components.css` (append)

- [ ] **Step 1: Append lightbox styles to `css/components.css`**

```css
/* Lightbox overlay */
.lightbox {
  position: fixed;
  inset: 0;
  background: rgba(26, 26, 26, 0.92);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1000;
  opacity: 0;
  pointer-events: none;
  transition: opacity 0.18s ease;
}
.lightbox[aria-hidden="false"] { opacity: 1; pointer-events: auto; }
.lightbox img { max-width: min(96vw, 1400px); max-height: 90vh; display: block; border-radius: 3px; box-shadow: 0 20px 60px rgba(0,0,0,0.4); }
.lightbox__close {
  position: absolute;
  top: 20px;
  right: 24px;
  font-size: var(--fs-xl);
  color: var(--bg);
  line-height: 1;
  padding: 8px 12px;
  cursor: pointer;
}
.lightbox__close:hover { color: #fff; }
```

- [ ] **Step 2: Create `js/lightbox.js`**

```javascript
(function () {
  // Build overlay via DOM methods
  const overlay = document.createElement('div');
  overlay.className = 'lightbox';
  overlay.setAttribute('aria-hidden', 'true');
  overlay.setAttribute('role', 'dialog');

  const closeBtn = document.createElement('button');
  closeBtn.className = 'lightbox__close';
  closeBtn.setAttribute('aria-label', 'Close');
  closeBtn.textContent = '\u2715'; // ✕
  overlay.appendChild(closeBtn);

  const imgEl = document.createElement('img');
  imgEl.alt = '';
  overlay.appendChild(imgEl);

  document.body.appendChild(overlay);

  function open(src, alt) {
    imgEl.src = src;
    imgEl.alt = alt || '';
    overlay.setAttribute('aria-hidden', 'false');
    document.body.style.overflow = 'hidden';
  }
  function close() {
    overlay.setAttribute('aria-hidden', 'true');
    document.body.style.overflow = '';
    setTimeout(() => {
      if (overlay.getAttribute('aria-hidden') === 'true') imgEl.src = '';
    }, 250);
  }

  // Event delegation — works for archive tiles, ASU tiles, dynamically rendered tiles
  document.addEventListener('click', (e) => {
    const tile = e.target.closest('.gallery-tile');
    if (tile) {
      const innerImg = tile.querySelector('img');
      const src = tile.dataset.full || (innerImg && innerImg.src);
      const alt = (innerImg && innerImg.alt) || tile.title || '';
      if (src) open(src, alt);
      return;
    }
    if (e.target === overlay || e.target === closeBtn) close();
  });

  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape' && overlay.getAttribute('aria-hidden') === 'false') close();
  });
})();
```

- [ ] **Step 3: Verify in browser**

Open `http://localhost:8000/work.html`. Click a tile → overlay opens with full image. Click outside image, click ✕, or press Escape → overlay closes.

Playwright MCP check:
```
navigate to http://localhost:8000/work.html
click first .gallery-tile
snapshot → .lightbox has aria-hidden="false"
press Escape
snapshot → .lightbox has aria-hidden="true"
```

- [ ] **Step 4: Commit**

```bash
git add js/lightbox.js css/components.css
git commit -m "feat(lightbox): add shared image overlay for gallery tiles"
```

---

## Phase 4 — Polish & Deploy

### Task 13: Responsive overrides

**Files:**
- Create: `css/responsive.css`
- Modify: all HTML files to include `responsive.css` after `components.css`

- [ ] **Step 1: Create `css/responsive.css`**

```css
/* Medium screens and down (≤ 900px) */
@media (max-width: 900px) {
  :root {
    --sp-6: 3rem;
    --sp-7: 4rem;
  }
  .cs-meta { gap: var(--sp-3); }
  .cs-grid-2 { grid-template-columns: 1fr; }
  .gallery-grid { grid-template-columns: repeat(auto-fill, minmax(180px, 1fr)); }
}

/* Small screens (≤ 600px) */
@media (max-width: 600px) {
  :root {
    --gutter: 1.25rem;
    --sp-5: 2rem;
    --sp-6: 2.5rem;
  }
  h1 { font-size: clamp(2rem, 10vw, 3.5rem); }
  .cs-header h1 { font-size: clamp(2rem, 9vw, 3rem); }
  .filter-chips { gap: 6px; }
  .chip { font-size: var(--fs-xs); padding: 5px 11px; }
  .case-study-tile__image { aspect-ratio: 4 / 3; }
}
```

- [ ] **Step 2: Add `<link>` to every HTML page after `components.css`**

Files to modify:
- `index.html` → add `<link rel="stylesheet" href="./css/responsive.css">`
- `about.html` → add `<link rel="stylesheet" href="./css/responsive.css">`
- `work.html` → add `<link rel="stylesheet" href="./css/responsive.css">`
- `asu-hub.html` → add `<link rel="stylesheet" href="./css/responsive.css">`
- `case-study/drive-insight.html` → add `<link rel="stylesheet" href="../css/responsive.css">`
- `case-study/app-redesign.html` → add `<link rel="stylesheet" href="../css/responsive.css">`
- `404.html` will add it in Task 14

- [ ] **Step 3: Preview at mobile widths**

Playwright MCP or browser dev tools:
```
browser_resize 375 x 812
navigate to each page
snapshot
```

Expected: readable at 375px, no horizontal scroll, filter chips wrap.

- [ ] **Step 4: Commit**

```bash
git add css/responsive.css index.html about.html work.html asu-hub.html case-study/
git commit -m "feat(css): add responsive overrides"
```

---

### Task 14: Update `404.html` to new visual system

**Files:**
- Modify: `404.html` (full rewrite)

- [ ] **Step 1: Replace `404.html`**

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Not found — Ninad Kulkarni</title>
  <link rel="icon" type="image/jpeg" href="./favicon.jpg">
  <link href="https://fonts.googleapis.com/css2?family=Instrument+Serif:ital@0;1&family=Inter:wght@400;500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="./css/tokens.css">
  <link rel="stylesheet" href="./css/base.css">
  <link rel="stylesheet" href="./css/components.css">
  <link rel="stylesheet" href="./css/responsive.css">
</head>
<body>
  <main class="container" style="padding-top: var(--sp-8); text-align: center;">
    <div class="label">404</div>
    <h1 style="margin-top: var(--sp-2);">Not found.</h1>
    <p class="muted" style="margin: var(--sp-3) auto 0; max-width: 480px;">This page doesn't exist — or it used to, and the 2020 site is gone.</p>
    <p style="margin-top: var(--sp-4);"><a href="./index.html" style="color: var(--accent);">&larr; Back to all work</a></p>
  </main>
</body>
</html>
```

- [ ] **Step 2: Preview**

Open `http://localhost:8000/404.html` directly.

- [ ] **Step 3: Commit**

```bash
git add 404.html
git commit -m "feat(404): rebuild not-found page on new visual system"
```

---

### Task 15: Meta tags & Open Graph

**Files:**
- Modify: every HTML file (append OG/Twitter meta inside `<head>`)

- [ ] **Step 1: For each HTML page, add OG meta after `<meta name="description">`**

For `index.html`:
```html
<meta property="og:type" content="website">
<meta property="og:title" content="Ninad Kulkarni — Designer">
<meta property="og:description" content="Designer and software engineer. Brand systems, print, and product UI.">
<meta property="og:image" content="./images/og-preview.jpg">
<meta name="twitter:card" content="summary_large_image">
```

Repeat per page with page-specific title + description. Case study pages reference `../images/og-preview.jpg`.

- [ ] **Step 2: Create a placeholder `images/og-preview.jpg`**

Flag to user: they should design a proper 1200×630 OG image. Temporary: use any wide existing image copied to this path. Do not commit a placeholder image if user will promptly produce one — leave the meta tag and let the image 404 until then.

- [ ] **Step 3: Commit**

```bash
git add index.html about.html work.html asu-hub.html case-study/ 404.html
git commit -m "feat(seo): add Open Graph and Twitter card meta"
```

---

### Task 16: Delete obsolete 2020 HTML + CSS (preserve media)

**Files to delete:**
- `achievements.html`, `skills.html`, `logos.html`, `image-manipulation.html`, `webapp.html`, `social-media.html`, `posters.html`, `tshirts.html`, `photography.html`, `animations.html`, `restorations.html`
- `css/home.css`, `css/page.css`, `css/responsive_home.css`
- `js/modal.js`

**Files to preserve (DO NOT delete):**
- Everything in `images/` (including `.psd` source files)
- Everything in `animations/`
- `favicon.jpg`

- [ ] **Step 1: Verify no current file references the doomed CSS/JS**

```bash
grep -r "home.css\|page.css\|responsive_home.css\|modal.js" --include="*.html" .
```

Expected: no results.

- [ ] **Step 2: Delete old files**

```bash
git rm achievements.html skills.html logos.html image-manipulation.html webapp.html social-media.html posters.html tshirts.html photography.html animations.html restorations.html
git rm css/home.css css/page.css css/responsive_home.css
git rm js/modal.js
```

- [ ] **Step 3: Verify images are still present**

```bash
ls images/logos/ images/Manip/ images/Photography/ images/Posters/ images/Social/ images/T-Shirts/ images/WebApp/ images/Restorations/
```

Expected: all folders intact.

- [ ] **Step 4: Preview every page for broken links**

Walk through home, about, work, asu-hub, both case studies, 404.

- [ ] **Step 5: Commit**

```bash
git commit -m "chore: delete obsolete 2020 HTML and CSS (images preserved)"
```

---

### Task 17: Update `CLAUDE.md` to reflect new stack

**Files:**
- Modify: `CLAUDE.md`

**CAUTION:** The user has an unstaged `CLAUDE.md` modification from before this work began. Coordinate with them before editing — their in-flight changes may conflict.

- [ ] **Step 1: Rewrite the architecture and commands sections**

New content should reflect:
- Pages: index, about, work, asu-hub, case-study/drive-insight, case-study/app-redesign, 404
- CSS layered: tokens → base → components → responsive
- `js/archive.js` (filter + render), `js/lightbox.js` (shared overlay)
- `data/work.json` as archive manifest
- Bootstrap removed, no jQuery, no build step, no package manager
- Hosting: GitHub Pages
- Preview: `python3 -m http.server 8000` (unchanged)

Remove the 2020 Bootstrap/Firebase/SRI paragraphs entirely.

- [ ] **Step 2: Commit**

```bash
git add CLAUDE.md
git commit -m "docs: update CLAUDE.md for 2026 stack"
```

---

### Task 18: Configure GitHub Pages

- [ ] **Step 1: Ensure repo has a GitHub remote**

```bash
git remote -v
```

If no remote:
```bash
git remote add origin git@github.com:Ninkuk/Ninad_Kulkarni_Portfolio.git
```

- [ ] **Step 2: Ask the user before pushing**

Do NOT push without explicit user approval — pushing is a visible action per instructions. Ask: "Ready to push to `origin/main`?"

If approved:
```bash
git push -u origin main
```

- [ ] **Step 3: Instruct user to enable Pages in repo settings**

Have the user:
1. GitHub → repo → Settings → Pages
2. Source: Deploy from a branch
3. Branch: `main`, folder: `/ (root)`
4. Save
5. Share the resulting URL

- [ ] **Step 4: Verify the live site**

At the Pages URL, walk through every page. Expect:
- All pages load
- All images load (relative paths resolve correctly at `/Ninad_Kulkarni_Portfolio/...` or at a custom domain)
- `work.html` archive loads and chips work
- Lightbox works
- 404 page shows on bad URL

- [ ] **Step 5: Remove Firebase files if tracked**

```bash
git ls-files | grep -E "\.firebaserc|firebase\.json" || echo "none tracked"
```

If tracked (they shouldn't be, but just in case):
```bash
git rm .firebaserc firebase.json
git commit -m "chore: remove Firebase references after migrating to GitHub Pages"
git push
```

---

## Self-Review Notes

**Spec coverage check:**
- § 1 (positioning, audience, principles) — covered via copy guidance in Tasks 3, 6, 7 + CLAUDE.md update in Task 17.
- § 2 (IA) — covered by Tasks 3, 4, 6, 7, 8, 10.
- § 3 (content strategy) — case study structure scaffolded in Tasks 6, 7 with explicit TODOs for copy and images.
- § 4 (visual system) — Tasks 1, 2, 5, 13 cover tokens, typography, layout, motion.
- § 5 (tech architecture) — file structure matches exactly; Tasks 1, 2, 5, 11, 12 build it.
- § 6 (phased rollout) — tasks map 1-to-1 onto spec phases.
- § 7 (out of scope) — honored (no CMS, no analytics, no dark mode).
- § 8 (deferred questions) — preserved; tagging ambiguity flagged in Task 9 for user resolution.

**Placeholder scan:**
- "TODO during execution" markers in Tasks 3, 6, 7, 8 for content authoring that requires user input (copy, real images). Scoped and labeled — not plan shirking.
- No "implement later" / "add appropriate X" — every structural step has concrete code.

**Type consistency:**
- `.gallery-tile` used in Tasks 8, 10, 11, 12 consistently.
- `data-full` attribute used in Tasks 8, 11, 12 consistently for lightbox source.
- `archive:rendered` custom event dispatched in Task 11; Task 12 uses event delegation so rebind not needed — consistent with how custom event is documented.
- Tag vocabulary in Task 9 matches Task 11's `TAG_LABELS` exactly (12 tags).

**Safety:**
- All DOM construction uses `createElement + textContent + setAttribute` rather than `innerHTML`. Owner-authored JSON is low-risk in practice, but the pattern also keeps the plan compatible with the repository's security hook.
- No `git push` without user approval (Task 18).

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-04-19-portfolio-2026.md`. Two execution options:

1. **Subagent-Driven (recommended)** — I dispatch a fresh subagent per task, review between tasks. Good for this plan because many tasks are self-contained file creation and a subagent can work independently without polluting the main conversation with HTML/CSS dumps.

2. **Inline Execution** — Execute tasks in this session using executing-plans, batch execution with checkpoints. Good if you want to pair on content decisions (case study copy, image selection, achievement list trim) as they come up.

**Which approach?**
